# Session — 2026-09-03

## Decisions
- Repo renamed `clementcopper/figma-cli` → **`figma-claude`**; the local checkout stays `~/figma-cli`
  because `~/.figma-ds-cli/bin/figma-cli` points into it by absolute path.
- Seven branches → **one trunk `master`**. `v2` and `archive/draft-v1` became the tags `v2-final`
  and `draft-v1`. The four PR branches stay on `origin` because deleting them closes the PRs.
- Upstream is **cherry-picked, not merged**. `README.md` is ours outright and is never reconciled.
- The Swift host is *the* app; `app/` (Electron) is the predecessor and is not developed further.
- Version **1.0.0**.
- CI: added `master` to the triggers — it had never run on a push here — and a macOS job that
  builds the Swift package and runs `CoreChecks`.
- The cwd button always shows the folder name; the Figma name gives way first.

## Open items
- `.claude/rules/` and the `CLAUDE.md` line pointing at it are uncommitted, and are Daniel's work,
  not mine. I appended today's learnings to `process.md` and `swift-host.md` but committed nothing.
- `CFBundleVersion` carries the git sha, so LaunchServices reads the version as "17.0". Reported;
  the About panel needs it there. No decision taken.

## Next concrete steps
- Review and commit `CLAUDE.md` + `.claude/rules/`.
- Open the app once and confirm the Finder icon in `swift-host/build/` is no longer the placeholder;
  `killall Finder` if it still is.

## Bugs fixed today
- Changing the working directory quit the app: `closeTab` takes the window down when the last tab
  goes, and the replace path closed before opening. The new tab is opened first now.
- The folder button showed the same directory for every tab: it was never redrawn on a tab switch.
  It is set in `show(_:)` now, where the status line already was.
- The folder name could vanish entirely at narrow widths; it is squeezed instead of dropped.
- The About panel could not run the CLI it had found — the shim needs `node` on PATH, and the child
  was spawned with the app's own environment.
