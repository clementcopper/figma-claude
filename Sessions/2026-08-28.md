# Session — 2026-08-28

Two days in one file: the feedback loop closed on 27.08., the Swift host ran from 27.08. into
28.08.

## Decisions

- **Feedback from the panel goes into `FEEDBACK.md`**, not `LEARNINGS.md`. The old rule
  ("append to LEARNINGS.md") could not work: the panel had read-only access to this checkout, so
  every entry cost a prompt, and raw friction notes would dilute a curated document. A
  `SessionStart` hook counts the open entries; `/feedback-triage` empties them.
- **The panel half lives in `~/.claude`, not in the repo** — the rule keyed on `$FIGMACLAUDE`
  and one narrow `Edit(…/FEEDBACK.md)` permission, both global so it works from any project.
  `bin/fig-feedback-setup` installs that half on a second machine; `.gitattributes` gives
  `FEEDBACK.md` `merge=union` so two machines appending do not conflict.
- **FigmaClaude gets a native host**, `swift-host/` on `feat/swift-host`. Go was the starting
  idea and was dropped: it has no native macOS UI, and the terminal emulator is the whole cost.
  Swift plus SwiftTerm measured ~10 MB/s throughput against Claude Code's 0.1–3 KB bursts,
  no measurable idle CPU, 2 MB bundle against 242 MB, 46 MB RSS against ~400 MB over five
  processes.
- **The Electron host stays until the Swift one is proven.** Both share
  `~/.figma-ds-cli/panel.json` and `panel-window.json`, so switching keeps the working directory
  and the window.
- **The status line producer is the app itself** (`FigmaClaude --statusline`). That removes the
  Node dependency the migration plan had listed as its one stumbling block.
- **Layout claims are measured, not eyeballed.** `--render-chrome`, `--render-statusline`,
  `--hover` and `measureRow()` exist because `screencapture` is blind here without Screen
  Recording permission.

## Open items

- **Dark mode works in the running app** (Daniel checked on 28.08.). The probe still renders the
  terminal white under a dark appearance — it has no window, so SwiftTerm never resolves the
  dynamic colour. `--appearance light|dark` now forces the palette for the probes; the chrome
  comes out right in both, the terminal rectangle stays white. The probe cannot see this class of
  fault, and that is the honest state of it.
- **`swift-host/` is staged, not committed.** ~4 400 lines, 192 checks green.
- The Figma menu is complete: bound file, Connect / Restart daemon / Stop daemon, Undo, Prepare
  this folder, Mode and Appearance — the six sections the Electron popover has. Appearance also
  sits under *View* in the menu bar. Still to check by hand in the running window: the daemon
  actions against a live Figma and the toast.
- One deliberate difference from Electron: **Connect stays disabled while Figma is connected**,
  because `figma-cli connect` can quit a running Figma.
- `cli-command` is ported, `limit-broadcast` and `zoom-factor` are not.
- The Electron `app/` still has 118 checks against the Swift host's 192 — the two suites overlap
  but are not yet in step.

## Next concrete steps

1. Ask Daniel what the running app shows in dark mode, then either finish or drop the colour
   work.
2. Commit `swift-host/` on `feat/swift-host` and push.
3. Decide whether the Swift host replaces `app/` or keeps living beside it — nothing in the repo
   forces the choice yet.
