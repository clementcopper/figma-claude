# Session — 2026-08-21

## Decisions

- **`DESIGN.md` kennzeichnet Schätzungen.** Base Unit als „_derived_" mit Herkunft statt als
  Anweisung unter „### Do", § States als „not read from this file", und gebundene
  Variablennamen (`brand-500`) statt erfundener (`accent-3`). Ausgelöst durch den Bericht der
  Code-Seite von designdone.de, die auf die geratene 2px-Basis hereingefallen war.
  Commit `4d8d201`, sechs neue Tests.
- **Regeln gehören nach `.claude/rules/`, nicht in `AGENTS.md`** — Claude Code liest AGENTS.md
  nicht (Doku geprüft). `init-agent --tool claude` schreibt jetzt dorthin, `--tool agents` bleibt
  für Codex, neues `--no-setup` lässt die `connect`-Sätze weg. Commit `7b16bc7`.
- **`Business/Design/` aufgelöst.** Alles vom Werkzeug Erzeugte liegt in
  `Business/FigmaClaude/`, die projektspezifischen Figma-Regeln in
  `Business/.claude/rules/figma-design.md`, Skill und Permissions in `Business/.claude/`.
  Sechs Verweise nachgezogen, `spec` findet die DESIGN.md am neuen Ort von allein.
- **`figma-cli` liegt über `~/.zshenv` auf PATH** (Shim aus `~/.figma-ds-cli/bin`). Nicht
  `.zshrc` — die liest zsh nur interaktiv.
- **Panel mit `claude-terminal-panel@61415dd` gleichgezogen**, `media/main.ts` und
  `media/styles.css` wieder byte-identisch. Commit `746867f`.
- **Doctor-Aufräumen:** 14 nie benutzte Skills abgeschaltet, doppelter Pre-Compact-Block in der
  globalen CLAUDE.md gekürzt, Upstream-Merge-Ablauf in einen Skill ausgelagert
  (~1.650 Tokens weniger pro Sitzung).

## Open items

- **`claude update` hängt.** Läuft seit ~10 Minuten ohne Ausgabe, Symlink weiter auf 2.1.237,
  während 2.1.238 **und** 2.1.239 bereits entpackt in `~/.local/share/claude/versions/` liegen.
  Passt zu `install_failed` aus `claude doctor`. Verdacht: derselbe Mechanismus, der gestern auf
  dem M2 `claude` aus dem Terminal genommen hat. Wenn reproduzierbar → `/bug`.
- **M2-Diagnose steht aus** (`claude doctor`, Symlink, `versions/` auf der anderen Maschine).
- **`variables export --json` ist halb gebaut** — Logik in `src/lib/variables-export.js`, kein
  Kommando, keine Tests. Ebenso offen: `mode set/clear` mit der Pin-Falle.
- **`export-jsx` / `export-storybook` sind kaputt** (`npx --yes figma-use`, seit 2.1.1 entfernt).
- Zwei uncommittete Änderungen im Arbeitsbaum: `CLAUDE.md` und
  `.claude/skills/upstream-merge/SKILL.md`.
- Ein Bericht an den Panel-Fork steht aus: `media/main.ts:1155` setzt `display: 'block'` inline
  und hebelt die eigene Zentrierung aus — `''` statt `'block'` wäre die saubere Reparatur.

## Next concrete steps

1. `git diff CLAUDE.md` ansehen und die zwei Doctor-Änderungen committen.
2. `claude --version` prüfen; hängt der Symlink weiter auf 2.1.237, `/bug` melden.
3. `variables export --json` verdrahten (Kommando in `src/commands/variables.js` + Tests), danach
   `mode set/clear`.
