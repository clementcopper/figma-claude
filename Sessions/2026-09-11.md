# Session — 2026-09-11

## Decisions
- Pipe Mode wird der Default auf macOS/Linux; Yolo bleibt als `connect --patch` (Legacy: laufendes Figma adoptieren, figjam-Canvas über Port, Windows).
- Alle vier Modi bleiben; Panel-Menü-Reihenfolge Pipe, Safe, Yolo, Browser. Safe = eigenständiger Compliance-Weg (reines Plugin-API), Browser orthogonal.
- Panel: schwebende Status-Karte statt Button-Toast; Erfolg bleibt bis Schließen; keine Emojis; alle Meldungen (auch Nudges) ins Overlay.
- `--no-wait` an `connect`, damit der Panel-Toast sofort den nächsten Schritt zeigt und der Watcher ihn grün macht.
- Kein asar-Backup. Signatur bleibt nach Patch ad-hoc; nur Reinstall/Update stellt Developer-ID wieder her (in SECURITY.md).
- `package.json` author → designdone; README-Urhebernennung an Sil + LICENSE bleiben (MIT).

## Open items
- Safe-Mode-Payload: Exporte als `Uint8Array` statt Number-Array (gemessen ~4x langsamer) — noch nicht umgesetzt.
- Windows: Pipe-fd-Vererbung ungetestet, dort weiterhin `--patch`.
- figjam-Canvas-Befehle brauchen einen Port → in Pipe nicht verfügbar (dokumentiert).
- Org-Plugin-Publish-Flow: Absatz aus Figma-Doku in SECURITY.md; ob "Only admin-approved plugins" auch Development-Plugins sperrt, sagt die Doku nicht.

## Next concrete steps
- App am echten Figma abnehmen: `open "swift-host/build/Figma Claude.app"`, Mode Pipe→Safe→Yolo→Pipe, Overlay + Punkte prüfen.
- Bei Weiterentwicklung: Safe-Mode-Binärpayload angehen (Plan § "Safe Mode messen").
