# Session — 2026-09-04

## Decisions
- swift-host: alte `StatusLineView` (636 Zeilen) + `--render-statusline` gelöscht, Git a28ecd1 behält sie.
- Spike-Reste (`ThroughputMeter`, `[spike]`-stderr, `traceSize`) raus; `MeteredTerminalView` → `PanelTerminalView`.
- Kind-Prozesse laufen durch `readOutput(_:from:timeout:)` — die einzige Stelle, die einen Timeout erzwingt.
- `countCompactions` scannt Bytes (`Data.range(of:)`, gemessen 14 ms statt 1 320 ms bei 17 MB).
- Fensterposition wird in `applicationWillTerminate` gespeichert, nicht mehr in `windowWillClose`.
- Feste Farbflächen gehen durch `TintedView` (`updateLayer`); `Hairline` ist eine davon.
- Version 1.0.1; 490 CoreChecks.

## Open items
- Live nicht geprüft: Appearance-Wechsel des Effort-Chips, Fenster-Resize ohne `windowDidResize`, Tab-Meldung bei fehlendem `claude`.
- CI-Lauf für 18502f3 noch nicht angesehen.

## Next concrete steps
- Daniel prüft die zwei UI-Punkte in der laufenden 1.0.1; bei Befund eigener `fix(swift-host)`-Commit.
