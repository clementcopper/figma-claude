
# Session — 2026-09-05

## Decisions
- Tier 2, Peripherie und die drei Konventionen (ID-Listen, delete, --json) umgesetzt; 80 tote FigmaClient-Methoden gelöscht; DESIGN.md liest #rgb und meldet unlesbare Farben.
- Smart-X-Einzeiler bleiben.
- Panel-Session als letzter Test nach einer Fix-Serie (neue Regel in process.md und global).

## Open items
- Sechs Panel-Befunde in FEEDBACK.md ## Open; Vorschlag 1/2/3/5 jetzt, 4/6 später, Antwort ausstehend.
- Daemon-Integrationstest flackert unter Suite-Last.

## Next concrete steps
- `/feedback-triage`, je Befund roter Test zuerst; Befund 5 erst reproduzieren.

## 06.09. 11:04 — Nachtrag: Feedback-Triage und Sessionnamen

## Decisions
- 22 Panel-Befunde: 21 gefixt/beantwortet, #5 war der zsh-Loop des Reporters (`$c` unsplit);
  Programmname auf allen Ladepfaden `figma-cli` (ae95e65).
- `--strict-vars` als Flag, Default unverändert; `node tree --json` liefert `tree` und `lines`;
  a11y contrast/text/touch Exit 1 bei Verstoß, audit bei error; `var list -c/-t/--json`.
- Sessionnamen (Swift-Host 1.1.0, f0a8308): `fc-<datei>-<seite>` beim Start, `fc-<w1>-<w2>` per
  Haiku und `/rename` nach dem ersten Prompt; eindeutig über Registry ∪ Ledger.

## Open items
- Rename im Betrieb beobachten (Daniel). Kandidat: erst ein Prompt mit ≥ 3 Inhaltswörtern benennen.
- Figmas 10-s-Meldung für fehlende IDs (sync `getNodeById` antwortet in 0,3 s) — nicht umgebaut.

## Next concrete steps
- Bei Hook-Zähler > 0: `/feedback-triage`.
